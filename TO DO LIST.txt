
o Implement block falling
system:

	- Create an array of
	X points, as all blocks
	will have the same 
	Y.
	
	- Give blocks random
	spawn times.

	- Make it so if block
	falls on head of player,
	heart is lost.

o Implement heart/lives system:

	- Get an Image for the hearts

	- Anchor in top left with canvas.

o Create the main menu.

o Implement block swap system:

	-
	

o Use the block object itself as the
object that calls the column it belongs
to's methods, give the block a collumn
gameobject.



END X: -20.3
MAX X: 20.3

Create an array of 
20 float containing
the X values, each
value differing by 2.
